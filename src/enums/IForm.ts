export type IForm = {
  card: string;
  cardholder: string;
  expiry: string;
  cvv: string;
  email: string;
  country: string;
  address: string;
  address2?: string;
  city: string;
  state: string;
  zip: string;
};